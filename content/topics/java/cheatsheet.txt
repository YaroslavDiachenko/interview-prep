📁 Java Language Fundamentals
    📌 Fundamentals. Variables & Types
        🔶 Primitive types
            🔹Default values
            🔹Wrapper classes
                    Using numeric values in object-oriented features - collections, generics, APIs that
                    require objects.
            🔹Autoboxing & Unboxing
            🔹Type Conversion & Casting. Widening (implicit) & Narrowing (explicit)
                    Cast large long to int. Truncates the higher-order bits. No exception is thrown.
                    Floating-point numbers do not wrap. Double.MAX_VALUE
            🔹Overflow & Underflow
                    When an integer overflows (exceeds maximum value) it rolls over to the minimum value (wraps around).
                    Does not throw an exception.
                    Silently wrap around.
                    To avoid: use Math (Math.addExact(), subtractExact(), multiplyExact(), etc.) or BigInteger
            🔹Floating-point precision loss
            🔹Floating-point precision loss

        🔶 Number
            🔹IntegerCache
            🔹NaN (Not a Number)
            🔹Positive/Negative Infinity
            🔹Floating-point equality pitfalls
            🔹BigInteger
                    Used to represent arbitrarily large integer values that cannot fit into Java's primitive integer types.
            🔹BigDecimal
                    Used instead of double for financial and high-precision calculations. Has variety of precision
                    and rounding modes.
            🔹Numeric literals (hex, octal, binary, underscore separators)

    📌 Fundamentals. Operators
    📌 Fundamentals. Control Flow

📁 JMM & JVM
    📌 JMM & JVM. Memory Footprint
        🔸Primitives
            byte (1 byte), char (2), short (2), int (4), long (8), float (4), double (8)
        🔸Reference (4 bytes with compressed Ordinary Object Pointers, 8 bytes otherwise)
        🔸Object Memory Layout (64-bit CPU Hotspot JVM)
            1. Header (16 bytes)
                - Mark Word: 8 bytes (identity hashcode, GC age, locking state)
                - Klass Pointer: 4 bytes (pointer to the class metadata in Metaspace)
                - Padding/Memory alignment (for objects) / Array length (for arrays): 4 bytes
            2. Data (variables or references)
        🔸Metrics (Shallow size, Deep size, Retained size (only referenced from this object))


📁 OOP
